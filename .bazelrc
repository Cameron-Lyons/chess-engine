build --cxxopt=-std=c++23
build --cxxopt=-Wall
build --cxxopt=-Wextra
build --cxxopt=-Wpedantic
build --copt=-march=native

build:release --copt=-O3 --copt=-DNDEBUG
build:debug --copt=-g --copt=-DDEBUG
build:avx2 --copt=-mavx2
